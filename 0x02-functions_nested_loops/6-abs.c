#include "holberton.h"

/**
 * _abs - computes the absolute value of an integer
 * @n: It needs an int for input
 *
 * Return: an int
 */

int _abs(int n)
{
	if (n < 0)
	{
		n = n * -1;
	}
	else
	{
	}
	return (n);
}
